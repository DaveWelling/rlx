export default () => {
    // StyledSummaryDetail is a side-by-side view of the summary and detail data
    // this isn't very useful on a mobile device with limited real estate.
    throw new Error('StyledSummaryDetail is not supported on React Native.');
};
